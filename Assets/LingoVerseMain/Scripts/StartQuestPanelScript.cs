using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartQuestPanelScript : MonoBehaviour
{
    // add action to toggle the panel instead of in npctriggertest and also add so when dialogue is finished to close it
}
